# SkillOpt Initial Skill

The `initial.md` file in this directory is retained directly from the
SkillOpt-derived codebase. SoftSkill keeps it for hard-skill baseline
evaluation, compatibility with existing configs, and comparison against
soft-prefix training runs.
